# Basic Macbook setup
- Living based on tools I might need.
- Generates ssh key sets some default settings
- Setups brew, node, python, and git, etc.
- Does the starship, zinit and zsh-defer loading to prevent new shells from taking forever to init
- Attached outcome `.zshrc` as example.
